{% extends 'base.html.twig' %}

{% block title %}Home - Artlet{% endblock %}

{% block content %}

{% if user %}

<div style="padding: 30px;">

    <h1>Art Catalog</h1>

    <div style="margin: 20px 0; display: flex; gap: 10px;">

        <input type="text"
               id="searchInput"
               placeholder="Search artworks..."
               style="flex: 1; padding: 10px; border: 1px solid #ccc; border-radius: 5px;">

        <select id="categoryFilter" style="padding: 10px; border-radius: 5px;">
            <option value="">All Categories</option>
            <option value="digital">Digital</option>
            <option value="painting">Painting</option>
            <option value="photography">Photography</option>
        </select>

        <button id="searchBtn"
                type="button"
                style="background: orchid; color: white; border: none; padding: 10px 15px; border-radius: 5px;">
            Search
        </button>

    </div>

    <div id="artGrid"
         style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px;">

        {% for art in artworks %}
            <a href="{{ basePath }}/art/{{ art.id }}" style="text-decoration:none; color:inherit;">
                <div class="art-card"
                    data-title="{{ art.title }}"
                    data-category="digital"
                    style="border:1px solid #ddd; padding:10px; border-radius:8px; cursor:pointer;">

                    <h3>{{ art.title }}</h3>
                    <p>{{ art.description }}</p>

                </div>
            </a>
        {% endfor %}

    </div>

</div>

{% else %}

<div style="text-align: center; padding: 80px 20px;">
    <h1>Login to browse Artlet</h1>
</div>

{% endif %}

{% endblock %}

{% block javascript %}
<script src="{{ basePath }}/assets/app.js"></script>
{% endblock %}